# Set these variables in the shell before running python app.py.
# This file is a template; it is not loaded automatically.
OPENAI_API_KEY=
LLM_MODEL=
LLM_BASE_URL=https://api.openai.com/v1
# LLM_API_KEY is also supported for compatible providers.
